# Cloudflare Configuration (for development)
# NOTE: Do NOT expose secret API tokens to client-side code.
# Use non-prefixed env var names (no VITE_ prefix) and set as secrets in
# Cloudflare Pages (Dashboard) or CI provider. Example placeholders below:
CLOUDFLARE_ACCOUNT_ID=your_account_id_here
# CLOUDFLARE_API_TOKEN should be set as a secret (do NOT put a real token here)
CLOUDFLARE_API_TOKEN=your_api_token_here
VITE_BASE_URL=/

# Environment (local development only)
NODE_ENV=development
