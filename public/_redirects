# Redirect all routes to index.html for client-side routing
# Ensure /index.html is served directly to avoid an infinite redirect loop
/index.html    /index.html    200
/*             /index.html    200
